// Написать программу масштабирования фигуры
/*
double[] x,y;
x=new double[10];
y=new double[10];
x[0]=0;
y[0]=0;
*/

class Programm
{
    class Point
    {
    public double x,y;
    {
        return Math.Sqrt(Math.Pow(A.x-B.x,2)+Math.Pow(A.y-B.y,2));
    }
    }
    static void Main()
{
Point a=new Point(),b=new Point();
a.x=0;
a.y=0;
b.x=2;
b.y=3;
System.Console.WriteLine(Distance(a,b));

Point[] figure=new Point[3];
double k=2;
for(int i=0;i<figure[i].y*k;)
}
}
// что то куча ошибок, вообще ничего не получилось